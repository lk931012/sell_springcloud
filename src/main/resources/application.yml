eureka:
  client:
    service-url:
      defaultZone:  http://localhost:8761/eureka
    register-with-eureka: false   #不显示eureka
#项目参数
spring:
  application:
    name: eureka      #项目名称
server:
  port: 8761          #项目端口